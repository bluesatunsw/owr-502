test_actuator:
  ros__parameters:
    type: forward_command_controller/ForwardCommandController
    joints: ["test_joint"]
    # This is the type of command interface (e.g. effort, position, velocity)
    interface_name: "velocity"